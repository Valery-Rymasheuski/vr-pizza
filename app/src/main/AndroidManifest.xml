<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rymasheuski.valery.vrpizza">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ShoppingCartActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".PlaceOrderActivity"
            android:parentActivityName=".ShoppingCartActivity" />
        <activity
            android:name=".NewsActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DeliveryActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AboutUsActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".OrderResultActivity" android:parentActivityName=".MainActivity"/>
    </application>

</manifest>